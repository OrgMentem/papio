bin/
dist/
extension/firefox/
extension/dev-unpacked/
extension/web-ext-artifacts/
extension/.ff-dev-profile/
extension/.env
.env
dist/release/
site/
node_modules/
*.log
__pycache__/
*.pyc
.DS_Store
.agents/*
!.agents/skills/
.agents/skills/*
!.agents/skills/papio-release/
# Go binaries built in the repo root (go build ./cmd/papio, ./cmd/docs-gen)
/papio
/docs-gen
.playwright-cli/

# Scratch tier: throwaway dev artifacts (oracle one-shots, etc.)
dev/scratch/

# identity-corpus capture output: before.txt/after.txt/report.json name
# every paper, author surname, and DOI in the operator's Zotero library —
# dev/scratch/ above already covers the recommended capture location, but
# these guard the repo root too in case a capture lands there by habit.
/before.txt
/after.txt
/report.json
.omp/rules/no-real-institutions-in-repo.md
